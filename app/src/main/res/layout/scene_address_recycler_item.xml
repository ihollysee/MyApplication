<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="80dp"
                android:layout_height="95dp">

    <View
            android:layout_centerInParent="true"
            android:layout_width="match_parent"
            android:layout_height="2px"
            android:background="#90cfff"/>

    <ImageView android:id="@+id/scene_address_icon"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:layout_centerInParent="true"
               android:src="@drawable/scene_address_normal"/>

    <TextView
            android:layout_marginTop="9dp"
            android:layout_below="@id/scene_address_icon"
            android:id="@+id/scene_address_name"
            android:layout_width="wrap_content"
            android:gravity="center"
            android:layout_centerHorizontal="true"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:maxWidth="82dp"
            android:textColor="#666666"
            android:textSize="12sp"/>
</RelativeLayout>
